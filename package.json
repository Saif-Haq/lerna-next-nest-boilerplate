{
  "name": "lerna-next-nest-boilerplate",
  "version": "1.0.0",
  "description": "mkdir lerna-monorepo cd lerna-monorepo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
